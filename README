This function takes responses (y), and 2 environmental variables (x1 & x2) 
along with prior parameters for beta (b0, Vbcoef) and sigma2 (s1,s2) and produces
estimates for beta and sigma using a Gibb's sampler and quadradic Bayesian regression
